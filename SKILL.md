---
name: transcript-to-doc
description: >-
  Convert 陈宇廷（Chen Yuting）/破晓 Dharma lecture transcript .docx files into formatted, proofread Word documents.
  Triggers when the user says "帮我整理文字稿", "帮我整理课程录音", "处理一下文字稿", or any equivalent request to process a lecture transcript into a polished document.
  Use when working with automatic transcription outputs that need: typo proofreading (Buddhist terminology), oral filler marking (gray strikethrough), topic-based paragraph splitting, hierarchical heading structure, and consistent font/formatting.
---

# 直播录音转写整理（Transcript to Doc）

## 1. 触发条件

当用户的输入包含以下关键词或意图时触发本 Skill：
- "帮我整理文字稿"
- "帮我整理课程录音"
- "处理一下文字稿"
- "整理直播文字"
- 其他类似的转写整理请求

同时要求：
- 原始文件为 `.docx` 格式的听录稿
- 听录稿中包含"人名+时间戳"的发言记录（如 `陳宇廷   0:04`）
- 如无法满足，向用户说明原因并终止

## 2. 输出格式

### 2.1 文件格式
- 输出文件：`.docx`（Word 文档）
- 文件命名：在原始文件名后添加 `-校对版` 后缀

### 2.2 字体规格

| 内容类型 | 字体 | 字号 | 颜色 | 其他格式 |
|---|---|---|---|---|
| 一级标题（一、二、三...） | 微软雅黑 | 5号（10.5pt） | 黑色 | 加粗；段前0行，段后0.5行，首行缩进2字符，单倍行距 |
| 二级标题（1.1、1.2、2.1...） | 微软雅黑 | 5号（10.5pt） | 黑色 | 加粗；段前0行，段后0.5行，首行缩进2字符，单倍行距 |
| 陈宇廷发言 | 宋体 | 5号（10.5pt） | 默认 | 人名和时间戳加粗；段前0行，段后3pt，首行缩进2字符，单倍行距 |
| 破晓/破曉发言 | 宋体 | 5号（10.5pt） | 默认 | 人名和时间戳加粗；段前0行，段后3pt，首行缩进2字符，单倍行距 |
| 其他人发言 | 仿宋 | 5号（10.5pt） | 默认 | 人名和时间戳加粗；段前0行，段后3pt，首行缩进2字符，单倍行距 |
| 灰色删除线标注 | 对应字体 | 5号（10.5pt） | 灰色 #B4B4B4 | 加删除线；其余同所在段落类型 |

### 2.3 发言标签格式

所有发言标签统一为：

```
发言人：(时间戳)正文
```

格式要求：
- 发言人与时间戳之间使用中文冒号 `：`
- 时间戳使用半角括号 `(...)`，不使用全角括号 `（...）`
- 时间戳后直接接正文，不留半角空格或全角空格
- 示例：`破晓：(0:28)嗯。`

### 2.4 段落基础格式
- 行距：单倍行距
- 段前：0 行
- 段后：标题为 0.5 行（约 7pt），正文为约 3pt
- 首行缩进：2 字符（约 0.85cm）

### 2.5 段落整理要求
- 行距：单倍行距（全部段落统一）
- 段前：0 行（全部段落统一）
- 段后：标题为 0.5 行（约 7pt），正文为约 3pt
- 首行缩进：2 字符（约 0.85cm，全部段落统一）
- 不能将一整段内容堆成一段（段落字数不超过 500 字）
- 不能一行一行堆叠，须按语意汇总成多个自然段

## 3. 处理流程

### Step 1：读取与校验
1. 确认用户提供的文件路径存在且为 `.docx` 文件
2. 若文件不存在或非 `.docx` 格式，向用户说明原因并终止
3. 读取文档中的所有段落（`python-docx` 的 `Document.paragraphs`）

### Step 2：删除图片与空段
1. 遍历文档中的所有内联形状（`inline_shapes`）和图片关联（`rels`），从文档中删除
2. 删除所有空段落（`text.strip()` 为空）

### Step 3：解析说话人与时间戳
1. 仅在前几行确认为元数据（会议标题、日期、时长、听录员信息）时跳过；如果开头已经是“人名+时间戳”，必须保留正文
2. 用正则匹配说话人标签：`^([\u4e00-\u9fff]{2,4})\s{2,}(\d{1,2}:\d{2}(?::\d{2})?)`
3. 提取（说话人, 时间戳, 正文）三元组
4. 只对**发言人标签中的时间戳**加半角括号，并在发言人与时间戳之间加中文冒号，格式如 `陳宇廷：(0:04)正文`
5. 正文中自然出现的时间戳不做修改
6. 发言人标签后不得插入空格或全角空格；时间戳右括号后直接接正文

### Step 4：转写矫正
1. **错别字校正**：根据佛法内容，批量替换错字和语音识别错误：

| 原文错误 | 修正为 |
|---|---|
| 四念柱 | 四念住 |
| 四正情 | 四正勤 |
| 四神族 | 四神足 |
| 涅盘 | 涅槃 |
| 大波涅盘经 | 大般涅槃经 |
| 声文成 | 声闻乘 |
| 小城 | 小乘 |
| 九成 | 九乘 |
| 灯地菩萨 | 登地菩萨 |
| 处理心/出理性 | 出离心 |
| 剧组 | 具足 |
| 正果 | 证果 |
| 上品上升/上身 | 上品上生 |
| 下品下身 | 下品下生 |
| 中品终身/终生 | 中品中生 |
| 下品终身/终生 | 下品中生 |
| 小节/小劫 | 小劫 |
| 大节 | 大劫 |
| 中节 | 中劫 |
| 16官制 | 十六观智 |
| 大制度论 | 大智度论 |
| 一切师地论 | 瑜伽师地论 |
| 阿拉汉国 | 阿罗汉果 |
| 须陀韩国 | 须陀洹果 |
| 司徒韩国 | 斯陀含果 |
| 金刚萨 | 金刚萨埵 |
| 马涛 | 马韬 |
| 杨晶/杨金 | 杨静 |
| 借香熏修 | 戒香熏修 |
| 升极乐国 | 生极乐国 |
| 毁毁谤 | 诽谤 |
| 出佛升学/初佛生雪 | 出佛身血 |
| 破和合神论 | 破和合僧 |

2. **书籍加书名号**：所有提到的佛经、论典名称，如《金刚经》《心经》《观无量寿经》《大智度论》《瑜伽师地论》《无量寿经》《俱舍论》《大般涅槃经》等，添加书名号。

**注意**：替换时遵循"长匹配在前"原则，避免短替换误伤长替换。例如"大波涅盘经"须在"涅盘"之前替换。

### Step 5：标注冗余内容（灰色+删除线）

以下类型的表达用**灰色（#B4B4B4）+ 删除线**标记，严禁删除任何文字：

| 类型 | 示例 |
|---|---|
| 填充词（独立段或行内） | 嗯、嗯嗯、嗯。、呃、啊、哦、唉、哎、哎呦、嗯哼、哼、呵 |
| 重复确认 | 对对对、是是是、嗯嗯嗯 |
| 无意义表达 | 然后。、好。、好了。、中品。 |
| 连接中断 | 哦，你，你查一下。、唉怎么黑的，看不清楚。、再来一次。 |
| 操作态表达 | 等一下，我把这个也找出来一下。、哦，对，我是我会贴给你们。 |
| 教学无关内容 | 麻烦你帮我拿一瓶，你给我的那个绿的那个那个植物的，谢谢。 |
| 结束语 | 大家刚才听了这些有些什么心得，继续要记得要写一些哦，尽量多写一些，谢谢大家。 |

**处理方法**：
- 完整段落匹配：如果整段文字完全匹配填充词列表，整段标记为灰色+删除线
- 仅语气词段落：如果一段话在 `发言人：(时间戳)` 后只剩 `嗯/啊/哦/呃/唉/哎/哼/呵` 等语气词和标点，则**整段**标记为灰色+删除线，包括发言人标签和时间戳。例如 `杨静：(9:07)嗯。` 中的 `杨静：(9:07)嗯。` 全部标灰色+删除线
- 片段匹配：如果正文中间包含非教学内容片段（如"麻烦你帮我拿一瓶"），只将该片段标记为灰色+删除线，其余部分保持正常
- 行内语气助词匹配：正文中夹杂的 `嗯/嗯嗯/呃/啊/哦/唉/哎/哎呦/嗯哼/哼/呵` 等口头填充词，也要单独切成 run 并标记为灰色+删除线
- 紧跟标点连带标注：如果灰色删除线的语气助词后面紧跟标点符号（如 `嗯。`、`啊？`、`哦，`），标点符号也一并标记为灰色+删除线，不能只标语气词本身
- 谨慎处理正常语法词：`吗`、`呢`、`吧` 这类句末语气词通常承担语法功能，不做批量灰色删除线标注，除非上下文明确是口头填充

### Step 6：主题切分、段落拆分与子标题生成

#### 6.1 通读全文，形成 4-10 个一级标题
按课程主题或内容逻辑切分，使用中文数字编号：
```
一、三十七道品总述
二、修行的捷径与三种可能性
三、罗汉果的修行
四、净土法门的本意
五、九品往生详解
六、人间修行更为珍贵
七、结语
```

#### 6.2 语义分段，添加二级标题
- 将大段内容按逻辑、语意层次拆分为多个自然段
- **拆分依据的优先级**：
  1. **主题标记拆分（主要）**：按预定义的主题转折标记切分，保证每段语义完整，如"我们再来看看十善业"、"好，下一个就是比较麻烦了的事情"、"那大乘佛法开始就对空性"等。
  2. **长度兜底拆分（次要）**：如果按主题标记拆分后仍有段落超过 500 字，才在最近的句号/问号/感叹号处断句。
- 主题标记来自两个来源：
  - `TOPIC_SPLIT_MARKERS` —— 全局主题转折标记（如"我们再来看看十善业"）
  - `TOPIC_TRANSITIONS` 的关键词列表 —— 细粒度子主题标记（如"那大乘佛法开始就对空性"），同时也用于插入子标题
- 提炼出二级标题，使用阿拉伯数字层级编号：
```
1.1 四念住与三十七道品的来历
1.2 现代人寻找捷径的心理
1.3 三种生命方向
2.1 罗汉果的实质与条件
...
```

#### 6.3 处理时间戳——仅首段保留

当一段长文本被拆分为多个子段落时，**只有第一个子段落**显示说话人+时间戳标签，后续的延续段落不再重复添加。这样既保留了原文的时间戳信息，又不会因为拆分段落而"新增"时间戳。

```
处理前（原文只有一条）：
  陳宇廷   3:55   三种生命方向...罗汉果...净土...

处理后（拆分后，仅首段有时间戳）：
  陳宇廷：(3:55)三种生命方向...苦...菩萨...
  那大乘佛法开始就对空性有不同的见解...  ← 无时间戳
  或者嫁到个又有钱又帅的男人...          ← 无时间戳
  罗汉这些级别可以看一下啊...            ← 无时间戳
```

实现方式：在输出子段落时，用 `enumerate` 遍历，`pi == 0` 时调用 `add_speech_split`（带标签），其余段落直接输出纯文本段落。

#### 6.4 内容语义子标题——基于关键词匹配

在时间戳相同的长段落内部，依赖时间戳无法区分不同主题。采用**关键词匹配**机制，在输出子段落时检测文字开头，匹配到预定义的关键词时，自动在段落前插入对应的子标题：

**注意：子标题关键词与拆分关键词是分离的。** 拆分段落只使用 `TOPIC_SPLIT_MARKERS`（重大主题转折），避免在细粒度子标题处过度切分导致段落破碎。`TOPIC_TRANSITIONS` 仅用于插入子标题，不参与文字拆分，保证段落连贯性。

#### 6.5 子标题的来源判定

子标题关键词来自两类来源：

**1. 手动定义的主题标记（主要）**：针对已知内容的关键转折点，如：
- 重大主题切换："那我是想到用一个治病的方法" → 教法总纲
- 关键概念引入："就是慢慢你开始身份转化了" → 身份转化
- 新阶段开启："好，这就进入了内三密了" → 内三密
- 总结性语句："好，那今天就讲到这儿" → 结语

**2. 通用转折模式（可跨文件复用）**：分析多份教学录音后发现的高频模式：
- `"那我是想到..."` → 新的讲授角度或比喻
- `"再来几个比喻呢"` → 通过类比深入讲解
- `"我们来看看"` → 展示资料或进入新示例
- `"第" + 数字 + "个叫"` → 逐一讲解阶段或步骤
- `"好，那今天就讲到这儿"` → 课程总结
- `"哦，最后等一下"` → 补充重要内容

**跨文件适用性说明**：由于每份录音的内容主题不同，`TOPIC_TRANSITIONS` 需要根据具体内容进行调整。建议处理新文件时：
1. 先通读全文，理解内容结构和主题切换点
2. 识别关键的转折句或引语
3. 将其添加为 TOPIC_TRANSITIONS 条目
4. 同时保留已验证的通用模式，这些模式在不同教学内容中重复出现

```python
# 当子段落开头匹配关键词时，插入对应的子标题
TOPIC_TRANSITIONS = [
    ("那大乘佛法开始就对空性", "2.3 大乘佛法的兴起与空性见解"),
    ("罗汉这些级别", "2.5 罗汉的各级果位"),
    ("也就是说上品上生", "2.8 上品上生的条件"),
    ("我们再来看看十善业", "4.2 十善十恶详解"),
    ("这个人下品下生", "5.2 下品下生——最劣条件者的去处"),
    ("所以现在开始修还是永远最重要的事情", "6.1 人间修行的珍贵"),
    ("对不同净土有不同的难易度", "7.1 净土的差异"),
    # ... 完整映射见 scripts/process_transcript.py
]
```

关键词优先匹配**长文本在前**，避免短词误触。已匹配的段落不再重复匹配同一关键词。

#### 6.5 合并换行符
- 删除正文中所有的硬换行符（`\n`），将一行一行的文字合并为连续的段落
- 只保留自然段落之间的分段，不保留原始听录稿中的单句换行

### Step 7：生成 Word 文档

使用 `python-docx` 按以下顺序生成输出文档：

1. 应用默认样式（宋体 10.5pt，单倍行距，段前0，段后7pt，首行缩进0.85cm）
2. 按时间戳顺序依次输出：

   | 要素 | 字体 | 字形 | 段前 | 段后 | 首行缩进 |
   |---|---|---|---|---|---|
   | 一级标题（一、二...） | 微软雅黑 10.5pt 黑色 | 加粗 | 0 | 7pt（0.5行） | 0.85cm |
   | 二级标题（1.1、2.1...） | 微软雅黑 10.5pt 黑色 | 加粗 | 0 | 7pt（0.5行） | 0.85cm |
   | 说话人标签 + 时间戳 | 宋体（宇廷、破晓/破曉）/ 仿宋（他人）10.5pt | 加粗；格式 `发言人：(时间戳)正文` | 0 | 3pt | 0.85cm |
   | 正文内容 | 宋体（宇廷、破晓/破曉）/ 仿宋（他人）10.5pt | 正常 | 0 | 3pt | 0.85cm |
   | 灰色删除线内容 | 同正文 | 加删除线 | 同正文 | 同正文 | 同正文 |

3. 陈宇廷和破晓/破曉发言用宋体，其他人用仿宋
4. 冗余内容字体颜色改为灰色（RGB 180,180,180）并添加删除线
5. 行内语气助词及其紧跟标点须拆成独立 run，并标为灰色+删除线
6. 写入文件并保存

## 4. 关键技术参考

### 4.1 字体设置（python-docx）

```python
from docx.shared import Pt, RGBColor
from docx.oxml.ns import qn

# 设置中文字体
run.font.name = '微软雅黑'
run.font.size = Pt(10.5)
run.font.color.rgb = RGBColor(0, 0, 0)
run.font.bold = True
run.element.rPr.rFonts.set(qn('w:eastAsia'), '微软雅黑')
```

### 4.2 段落格式设置

```python
from docx.enum.text import WD_LINE_SPACING

pf = paragraph.paragraph_format
pf.space_before = Pt(0)
pf.space_after = Pt(7)  # 约 0.5 行
pf.line_spacing_rule = WD_LINE_SPACING.SINGLE
pf.first_line_indent = Cm(0.85)
```

### 4.3 时间戳正则

```python
# 匹配 "陳宇廷   0:04" 或 "骆发智   1:48:29"
PAT_SPEAKER = re.compile(r'^([\u4e00-\u9fff]{2,4})\s{2,}(\d{1,2}:\d{2}(?::\d{2})?)')
```

### 4.4 灰色删除线

```python
run.font.color.rgb = RGBColor(180, 180, 180)
run.font.strike = True
```

## 5. 错误处理

| 场景 | 处理方式 |
|---|---|
| 文件不存在 | 向用户说明找不到该文件，请提供正确路径 |
| 非 .docx 格式 | 向用户说明仅支持 .docx 格式 |
| 文档无内容 | 向用户说明文档为空或无法解析 |
| 无说话人时间戳 | 向用户说明该文档可能不是标准听录格式 |
| python-docx 加载失败 | 向用户说明文件可能已损坏 |
| 处理过程中异常 | 捕获异常并说明原因，终止处理 |

## 6. 验证要点

处理完成后，验证以下内容：
- [ ] 所有图片已删除，无空段
- [ ] 发言标签格式为 `发言人：(时间戳)正文`
- [ ] 时间戳使用半角括号，且仅限发言人标签
- [ ] 时间戳后没有半角空格或全角空格
- [ ] 所有错别字已校正
- [ ] 无意义表达已用灰色+删除线标记
- [ ] 仅由发言标签、时间戳、语气词和标点组成的段落已整段灰色+删除线
- [ ] 行内语气助词已用灰色+删除线标记
- [ ] 灰色删除线语气词后紧跟的标点也已一并标记
- [ ] 段落连续排列（无硬换行符）
- [ ] 段落字数不超过 500 字
- [ ] 有 4-10 个一级标题
- [ ] 有对应数量的二级标题
- [ ] 所有二级标题（如 1.1、1.2）均为加粗
- [ ] 字体和段落格式正确
- [ ] 发言者为破晓/破曉的所有发言均为宋体5号字（10.5pt）
- [ ] 内容完整未被删减
